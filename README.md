# aerial-crowd-strip-count
Estimate crowd sizes from drone videos that fly along a street — works even on screen-recorded Instagram reels. Density-model counting + optical-flow strip summing, so every person is counted exactly once.
